[config]
# Topology parameter should follow either EGS_topology.py or BHS_topology.py
# Platform parameter in: [SPR, EMR, GNR]
# PCIE devices should be split by ","
# Degraded topology is reserved for auto scripts to fill
topology = TOPOLOGY_8S_Perf_Opt_Config_1
degraded_top =
platform = SPR
pcie = I210

